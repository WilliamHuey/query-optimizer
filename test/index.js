var queryToTopology = 'undefined' == typeof window
  ? require('..')
  : require('tower-query-to-topology'); // how to do this better?

var assert = require('assert');

describe('queryToTopology', function(){
  it('should test', function(){
    
  });
});
